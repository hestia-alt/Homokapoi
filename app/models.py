"""
Models for the app.

Note: We're using Supabase for data storage, so we don't define
traditional Django models here. The database schema is managed
in Supabase directly.

See ARCHITECTURE.md for the Supabase schema.
"""
from django.db import models

# No models - using Supabase

